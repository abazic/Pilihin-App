'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import {createClient} from '@/lib/supabase/client';
export default function Register(){const [f,setF]=useState({username:'',name:'',email:'',whatsapp:'',password:''}),[error,setError]=useState(''),[loading,setLoading]=useState(false);const router=useRouter();const supabase=createClient();async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const {error}=await supabase.auth.signUp({email:f.email,password:f.password,options:{data:{username:f.username,full_name:f.name,whatsapp:f.whatsapp}}});if(error){setError(error.message);setLoading(false);return;}router.push('/login');}return <main className="min-h-screen flex items-center justify-center p-6 bg-gray-50"><form onSubmit={submit} className="w-full max-w-md bg-white p-8 rounded-2xl border space-y-4"><h1 className="text-2xl font-semibold">Daftar Client</h1>{(['username','name','email','whatsapp','password'] as const).map(k=><input key={k} type={k==='password'?'password':k==='email'?'email':'text'} required value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})} placeholder={k} className="w-full p-3 border rounded-lg"/>)}{error&&<p className="text-red-600 text-sm">{error}</p>}<button disabled={loading} className="w-full py-3 bg-black text-white rounded-xl">{loading?'Mendaftar...':'Daftar'}</button></form></main>}