'use client';import{useState}from'react';import{createClient}from'@/lib/supabase/client';import{useRouter}from'next/navigation';export default function Reset(){const[p,setP]=useState(''),[msg,setMsg]=useState(''),[err,setErr]=useState('');const r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const{error}=await createClient().auth.updateUser({password:p});if(error)setErr(error.message);else{setMsg('Password berhasil diubah.');setTimeout(()=>r.push('/login'),800)}}return <main className="min-h-screen flex items-center justify-center p-6 bg-gray-50"><form onSubmit={submit} className="w-full max-w-md bg-white p-8 rounded-2xl border space-y-4"><h1 className="text-2xl font-semibold">Password Baru</h1><input type="password" minLength={8} required value={p} onChange={e=>setP(e.target.value)} placeholder="Password baru" className="w-full p-3 border rounded-lg"/>{err&&<p className="text-red-600 text-sm">{err}</p>}{msg&&<p className="text-green-600 text-sm">{msg}</p>}<button className="w-full py-3 bg-black text-white rounded-xl">Simpan</button></form></main>}